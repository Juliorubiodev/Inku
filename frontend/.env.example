# Frontend Environment Variables

# ============================================
# API Configuration (Development)
# ============================================
# For local development with backend running on different ports
VITE_API_BASE_URL=http://localhost:8001
VITE_AUTH_API_URL=http://localhost:8003
VITE_LIST_API_URL=http://localhost:8002

# For production-like local (docker-compose.prod.yml with Nginx):
# Leave empty for same-origin requests
# VITE_API_BASE_URL=
# VITE_AUTH_API_URL=
# VITE_LIST_API_URL=

# ============================================
# Firebase Configuration (REQUIRED)
# ============================================
# Get these from Firebase Console:
# Project Settings > Your Apps > Web App > Config
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890

# ============================================
# Setup Instructions
# ============================================
# 1. Copy this file to .env.local (for dev) or .env (gitignored)
#    cp .env.example .env.local
#
# 2. Replace placeholder values with your actual Firebase config
#
# 3. Restart Vite dev server (npm run dev)
#    Note: Vite does NOT hot-reload .env changes!
#
# 4. For production build, use .env.production with empty API URLs
